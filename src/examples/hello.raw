oper someOperation(a, b) {
     if a + b >= 10 {
     	return a + b.
     } else {
        someOperation(a + 1, b + 1).
     }
}

oper main () {
     // this is comment

     var name = "Monster".
     var varWithVar = name.
     var num = 123.123.
     var emptyVar = nil. // this is allowed

     print("hello world").
     print(1 + 2). // prints 3
     print(name).

     var some = true;

     // while loop
     prolonging some {
     	whether 1 > 0.4 {
	   print("One is greater that 0.4").
	}
	some = false.
     }

     var a = 1.
     var b = 1.
     print(someOperation(a, b)). // prints 20
}
